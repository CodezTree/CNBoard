<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.teammgh.cnboard">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="whateverString"
        android:icon="@mipmap/ic_launcher_cnboard"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:appComponentFactory">
        <activity android:name=".FoodDetail"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".NoticeDetailActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".SplashActivity"></activity>
        <activity android:name=".EnrolmentSubject">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LakeViewActivity" />
        <activity android:name=".InputSubject"></activity>
        <activity android:name=".DdayActivity"></activity>
        <activity android:name=".DdaySettingActivity" />
    </application>

</manifest>